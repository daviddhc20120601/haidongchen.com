// Google Analytics bootstrap. Kept as an external file (rather than an inline
// <script> in index.html) so the Content-Security-Policy can forbid inline
// scripts entirely.
window.dataLayer = window.dataLayer || [];
function gtag() {
  window.dataLayer.push(arguments);
}
gtag('js', new Date());
gtag('config', 'G-C8L92KRNJE');
